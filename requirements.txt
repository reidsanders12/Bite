flet==0.28.3
# ft.AudioRecorder was split out of core flet into this separate package
# starting around 0.26.0; the compiled build's Flutter template no longer
# bundles it, so voice logging needs it installed explicitly.
flet-audio-recorder==0.1.0
python-dotenv==1.2.1
# 2.13.4 pulls in pydantic-core==2.46.4, which (like opencv-python 5.0.0.93
# above) has no published Android wheel and breaks `flet build apk`.
# 2.13.3 pulls in pydantic-core==2.46.3, which does have one.
pydantic==2.13.3
httpx==0.28.1
supabase==2.31.0
# Camera capture + barcode/QR scanning (Snap & Log, Post a Meal, Progress
# Photos, barcode lookup in app/views/*.py). Used to be opencv-python, which
# has NO iOS build at all -- confirmed both by Apple App Store Connect
# rejecting the archive ("Invalid bundle structure... cv2.cv2.framework...
# type BUNDLE is not valid") and by flet-dev/flet#4850. Replaced with a
# purpose-built local package wrapping the Flutter `camera` + `mobile_scanner`
# plugins (both have real iOS + Android support). Never published to PyPI --
# a local `file://` reference, not a dev_packages entry, because this app
# uses requirements.txt for its dependencies, not [project.dependencies] in
# pyproject.toml, and flet_cli's dev_packages substitution only fires for
# the latter (confirmed by reading flet_cli/commands/build.py directly).
flet-native-camera @ file:///Users/reidsanders/Bite/flet-native-camera

# Pinned explicitly (not just left to pip's transitive resolution): flet==0.28.3's
# socket layer throws "Receive loop error: 'text'" against any non-localhost
# client (e.g. `flet run --ios`/`--android`, or `--web` accessed from another
# device) when `websockets` resolves to 14.x or newer. 13.0.1 is the newest
# version confirmed to still work. See flet-dev/flet issues #4884 and #5603.
websockets==13.0.1

# Health App integration (Profile -> Connect Health App): wraps the Flutter
# `health` package to read Apple HealthKit (iOS) / Health Connect (Android)
# data. Requires flet>=0.25.2, which flet==0.28.3 above satisfies.
# flet-permission-handler is its stated companion dependency for the
# runtime OS permission prompts. Only meaningful on iOS/Android builds --
# see app/health_engine.py and app/views/health_view.py.
flet-health==0.1.2
flet-permission-handler==0.1.0

# Sponsor card "Redeem" QR code (app/qr_engine.py). Pure-Python by default
# (no Pillow) -- same Android-wheel caution as opencv-python above, so we
# stick to the qrcode.image.pure backend rather than adding Pillow too.
qrcode==7.4.2
